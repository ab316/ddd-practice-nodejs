version: '3'
services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    working_dir: /app
    command: yarn start:dev
